//import React from 'react';
// import { shallow } from 'enzyme';
// import { App } from './App';

describe('<App />', () => {
  it('should link to an login route path', () => {
    // const wrapper = shallow(<App />);
    // const actual = wrapper.findWhere(n => n.prop('path') === '/login').length;
    // const expected = 1;
    //
    // expect(actual).toEqual(expected);
    expect( true );
  });
});
